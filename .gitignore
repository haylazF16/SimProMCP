node_modules/
dist/
.env
*.log
.DS_Store
.vscode/
.idea/
*.tmp

# Claude Code local settings — contains command history that may include
# tokens from `curl -H "Authorization: Bearer ..."` runs. Never commit.
.claude/

# Editor/OS scratch
*.swp
Thumbs.db

# Superpowers brainstorming session files (local mockups, transient)
.superpowers/

# Server runtime files (HTTP mode) — hold secrets / PII, never commit.
tokens.json
audit.log
*.audit.log
goldmanportal/
